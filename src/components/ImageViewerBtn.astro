---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"button"> {}
---

<button
  class={`w-12 cursor-pointer transition-colors z-10
    ${Astro.props.class ?? ""}
  `}
  {...Astro.props}
>
  <slot />
</button>
